# Prompt finali dei mockup

## Variante C compatta — soluzione scelta

Riferimento: variante-c.png. Output: variante-c-compatta.png. Strumento integrato imagegen; richiesta successiva dell'utente: indicatore della C, altezza compatta della A, nomi in inglese.

```text
Use case: ui-mockup. Edit target is the attached TurboCode composer mockup. Create the final refined variant C with the low footer height and compactness of a native single-line macOS status bar. Keep white rounded composer, pale background, subtle separator, dark readable SF Pro-style text, blank input area, branch main at far left, EXACTLY ONE plain borderless model dropdown "Llama-server · Medium" and blue circular send arrow ABOVE the divider in the text input area. Preserve those positions. Title outside card "Compact context indicator". Placeholder "What would you like to do in this project?".
PRIMARY CHANGE: shrink the bottom footer height to about 44 logical points (approximately HALF the height of the old footer). Keep all labels and their values INLINE, never separate large stacked metric columns. A single compact horizontal footer row:
branch icon "main" chevron | "Context" semibold "38%" and readable "24,576 / 65,536" with a very thin 3-point blue capacity bar directly beneath only this context group, filled 38 percent | "Cache hit" bold "72%" | "Session tokens" bold "148,320".
Labels EXACTLY "Context", "Cache hit", "Session tokens", all English. Label font 13pt, value font 14pt semibold, no oversized 20pt metric numbers. Context label, percentage, fraction on one line; gauge tucked just under that line with 3pt gap. Cache hit and Session tokens remain inline label-value pairs centered vertically. Footer vertical padding 7pt; no excess blank height. Main branch gets no large separate column. Subtle short vertical dividers. Labels medium dark gray, digits near-black. Keep metrics flat read-only text, not buttons. No left model menu, no spinner. Numbers illustrative. Tiny outside note "Illustrative data". Wide landscape close-up, sharp UI screenshot style. Preserve ample whitespace in entry area but reduce card's total height by reducing footer; the footer must look genuinely compact.
```


Strumento: imagegen integrato. Tre generazioni distinte con lo screenshot dell'utente come riferimento visivo. Variante A rigenerata per mantenere modello e invio sopra il divisore. Nessun codice UI è stato generato o implementato.

## Variante A

```text
Use case: ui-mockup. Generate a high-fidelity native macOS SwiftUI composer design mockup for TurboCode. Input image is a visual reference only: focus on the lower composer, do not reproduce its conversation messages. Wide landscape close-up 1536x640 approximately, light mode, pale macOS window background, a single large white rounded composer card occupying most of canvas, subtle separator and shadow, crisp SF Pro-like typography, calm Apple HIG-inspired layout. Above card tiny design title. Text entry invitation exactly "Cosa vuoi fare in questo progetto?". Generous blank editable area. On the second line at the right preserve exactly ONE model dropdown labelled "Llama-server · Medium" with chevron, then a blue circular send arrow. Remove the left laptop/Llama dropdown entirely. Keep one Git branch menu "main" with branch icon and chevron. Replace small unlabeled context ring with permanently visible text statistics. Three metrics, exact text labels "Contesto", "Cache hit", "Token sessione". Example values: context "24.576 / 65.536" and "38%"; cache "72%"; session tokens "148.320". These are illustrative values, not real telemetry. Footer labels must be readable medium-dark gray, values near-black semibold with tabular-looking digits. Statistics are read-only, do not put chevrons on them and do not make them look like buttons. No marketing art, no device frame, no code, no new toolbars. Display a tiny note outside card "Dati illustrativi". All labels Italian except existing model name and Cache hit.
Variant A, title "A · Barra compatta". Bottom footer about 52 logical points tall, a single horizontal row: at far left branch menu main, a thin vertical separator, then Contesto 38% with 24.576 / 65.536 in readable smaller text, then Cache hit 72%, then Token sessione 148.320. All three metrics visible along the row, clear spacious grouping without colored cards, no gauges. Preserve low composer height.
Critical layout: model selector and send button MUST be ABOVE the horizontal divider in their own row at the lower right of the text entry area, like the original reference. Footer below divider contains only branch and statistics, NO model controls. In this compact variant labels and values are INLINE on one line, such as Contesto 38% · 24.576 / 65.536, Cache hit 72%, Token sessione 148.320. Not labels above values. Keep everything spacious and crisp.
```

## Variante B

```text
Use case: ui-mockup. Generate a high-fidelity native macOS SwiftUI composer design mockup for TurboCode. Input image is a visual reference only: focus on the lower composer, do not reproduce its conversation messages. Wide landscape close-up 1536x640 approximately, light mode, pale macOS window background, a single large white rounded composer card occupying most of canvas, subtle separator and shadow, crisp SF Pro-like typography, calm Apple HIG-inspired layout. Above card tiny design title. Text entry invitation exactly "Cosa vuoi fare in questo progetto?". Generous blank editable area. On the second line at the right preserve exactly ONE model dropdown labelled "Llama-server · Medium" with chevron, then a blue circular send arrow. Remove the left laptop/Llama dropdown entirely. Keep one Git branch menu "main" with branch icon and chevron. Replace small unlabeled context ring with permanently visible text statistics. Three metrics, exact text labels "Contesto", "Cache hit", "Token sessione". Example values: context "24.576 / 65.536" and "38%"; cache "72%"; session tokens "148.320". These are illustrative values, not real telemetry. Footer labels must be readable medium-dark gray, values near-black semibold with tabular-looking digits. Statistics are read-only, do not put chevrons on them and do not make them look like buttons. No marketing art, no device frame, no code, no new toolbars. Display a tiny note outside card "Dati illustrativi". All labels Italian except existing model name and Cache hit.
Variant B, title "B · Valori in evidenza". Footer about 80 logical points tall, at left branch main centered vertically, then thin vertical separator and three evenly spaced read-only metric columns integrated into one footer surface. Each column has a readable label above a large semibold value. Contesto value 38% with smaller 24.576 / 65.536 underneath, Cache hit large 72%, Token sessione large 148.320. Hairline vertical dividers between columns; no individual pill backgrounds, no buttons or charts. This option prioritizes legibility.
Critical layout: the model selector and send button are ABOVE the horizontal divider, inside the text input section in their own row, as in the reference screenshot. The footer below divider contains only branch and statistics. Use this exact separation.
```

## Variante C

```text
Use case: ui-mockup. Generate a high-fidelity native macOS SwiftUI composer design mockup for TurboCode. Input image is a visual reference only: focus on the lower composer, do not reproduce its conversation messages. Wide landscape close-up 1536x640 approximately, light mode, pale macOS window background, a single large white rounded composer card occupying most of canvas, subtle separator and shadow, crisp SF Pro-like typography, calm Apple HIG-inspired layout. Above card tiny design title. Text entry invitation exactly "Cosa vuoi fare in questo progetto?". Generous blank editable area. On the second line at the right preserve exactly ONE model dropdown labelled "Llama-server · Medium" with chevron, then a blue circular send arrow. Remove the left laptop/Llama dropdown entirely. Keep one Git branch menu "main" with branch icon and chevron. Replace small unlabeled context ring with permanently visible text statistics. Three metrics, exact text labels "Contesto", "Cache hit", "Token sessione". Example values: context "24.576 / 65.536" and "38%"; cache "72%"; session tokens "148.320". These are illustrative values, not real telemetry. Footer labels must be readable medium-dark gray, values near-black semibold with tabular-looking digits. Statistics are read-only, do not put chevrons on them and do not make them look like buttons. No marketing art, no device frame, no code, no new toolbars. Display a tiny note outside card "Dati illustrativi". All labels Italian except existing model name and Cache hit.
Variant C, title "C · Contesto con indicatore". Footer about 88 logical points tall: far left branch main; separated to its right an expanded Context group taking 45 percent of remaining width, label Contesto with bold 38% and readable 24.576 / 65.536, below a very thin blue static capacity gauge filled exactly 38 percent on neutral gray track. At right two plain text groups Cache hit 72% and Token sessione 148.320 with semibold prominent values. Label and digits always visible. The gauge shows capacity, no spinner, no animation.
Critical layout: the plain borderless model selector and send button are ABOVE the horizontal divider, inside the text input section in their own row, as in reference. Footer below divider contains only branch and statistics. Keep labels and metrics flat and native.
```
